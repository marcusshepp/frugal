Creating new Category

<%= form_for(@category) do |f| %>
    <%= f.label :name  %>
    <%= f.text_field :name %>
    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.submit "Submit" %>
<% end %>
